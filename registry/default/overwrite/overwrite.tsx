"use client"

import { createCheezMark } from "../cheez-core/create-cheez-mark"
import { overwriteDefinitions } from "./overwrite.definition"

export const Overwrite = createCheezMark(
  "Overwrite",
  overwriteDefinitions,
  "#151515",
)
